{
  "name": "basketball-coach-strategy-functions",
  "version": "1.0.0",
  "description": "練習メニュー配信 Cloud Function（Firestore → 既存エンジン/描画 → HTML）",
  "type": "module",
  "main": "dist/index.mjs",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "npx -y esbuild@0.24.0 index.mjs --bundle --platform=node --format=esm --packages=external --outfile=dist/index.mjs"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^13.7.0",
    "firebase-functions": "7.2.5"
  }
}
